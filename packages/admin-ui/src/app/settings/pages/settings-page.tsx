import { SettingsPageLayout } from '@/lib/components';

export const SettingsPage = () => {
  return (
    <SettingsPageLayout title="Shipments" subtitle="Manage your shipment methods">
      <h1>Settings page</h1>
    </SettingsPageLayout>
  );
};
