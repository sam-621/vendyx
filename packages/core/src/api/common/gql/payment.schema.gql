type Payment implements Node {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  transactionId: String
  amount: Int!
  method: String!
}

type PaymentList implements List {
  items: [Payment!]!
  count: Int!
}
